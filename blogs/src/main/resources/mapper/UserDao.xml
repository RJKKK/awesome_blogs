<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.blog.security.core.dao.UserDao">
	
	<!-- 查询用户的所有权限 -->
	<select id="selectMenuByUserId" resultType="com.blog.security.core.entity.MenuEntity" parameterType="int">
		SELECT DISTINCT m.* FROM user_role ur
			LEFT JOIN role_menu rm ON ur.role_id = rm.role_id
			LEFT JOIN menu m ON rm.menu_id = m.menu_id
		WHERE ur.user_id = #{userId}
	</select>

	<!-- 查询用户所有角色 -->
	<select id="selectRoleByUserId" resultType="com.blog.security.core.entity.RoleEntity" parameterType="int">
		SELECT sr.* FROM role sr
		LEFT JOIN user_role se ON se.role_id = sr.role_id
		WHERE se.user_id = #{userId}
	</select>



</mapper>